@model CorporateCMS.Models.Announcement

@{
    ViewData["Title"] = "Duyuru Düzenle";
}

<div class="container-fluid">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">@ViewData["Title"]</h1>
        <a href="@Url.Action("Index")" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
            <i class="fas fa-arrow-left fa-sm text-white-50"></i> Geri Dön
        </a>
    </div>

    <div class="row">
        <div class="col-lg-8">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">Duyuru Bilgileri</h6>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" enctype="multipart/form-data">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="CreatedDate" />
                        <input type="hidden" asp-for="CreatedBy" />
                        <input type="hidden" asp-for="ViewCount" />
                        
                        <div class="form-group">
                            <label asp-for="Title" class="control-label">Başlık</label>
                            <input asp-for="Title" class="form-control" placeholder="Duyuru başlığını girin" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Summary" class="control-label">Özet</label>
                            <textarea asp-for="Summary" class="form-control" rows="3" placeholder="Kısa özet yazın"></textarea>
                            <span asp-validation-for="Summary" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Content" class="control-label">İçerik</label>
                            <textarea asp-for="Content" class="form-control tinymce-editor" rows="10" placeholder="Duyuru içeriğini yazın"></textarea>
                            <span asp-validation-for="Content" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label for="imageFile" class="control-label">Resim</label>
                            @if (!string.IsNullOrEmpty(Model.ImagePath))
                            {
                                <div class="mb-2">
                                    <img src="@Model.ImagePath" alt="Mevcut resim" class="img-thumbnail" style="max-width: 200px;" />
                                    <p class="small text-muted">Mevcut resim</p>
                                </div>
                            }
                            <input type="file" name="imageFile" id="imageFile" class="form-control" accept="image/*" />
                            <small class="form-text text-muted">Yeni resim seçmezseniz mevcut resim korunacak.</small>
                            <input type="hidden" asp-for="ImagePath" />
                        </div>

                        <div class="form-group">
                            <label asp-for="Tags" class="control-label">Etiketler</label>
                            <input asp-for="Tags" class="form-control" placeholder="Virgülle ayırarak etiketler girin" />
                            <span asp-validation-for="Tags" class="text-danger"></span>
                            <small class="form-text text-muted">Örnek: teknoloji, haberler, duyuru</small>
                        </div>

                        <div class="form-group">
                            <label asp-for="PublishDate" class="control-label">Yayın Tarihi</label>
                            <input asp-for="PublishDate" class="form-control" type="datetime-local" 
                                   value="@(Model.PublishDate?.ToString("yyyy-MM-ddTHH:mm") ?? DateTime.Now.ToString("yyyy-MM-ddTHH:mm"))" />
                            <span asp-validation-for="PublishDate" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <div class="form-check">
                                <input asp-for="IsPinned" class="form-check-input" type="checkbox" />
                                <label asp-for="IsPinned" class="form-check-label">
                                    Sabitlenmiş Duyuru
                                </label>
                            </div>
                            <small class="form-text text-muted">Sabitlenmiş duyurular en üstte görünür.</small>
                        </div>

                        <div class="form-group">
                            <div class="form-check">
                                <input asp-for="IsActive" class="form-check-input" type="checkbox" />
                                <label asp-for="IsActive" class="form-check-label">
                                    Aktif
                                </label>
                            </div>
                        </div>

                        <div class="form-group mt-4">
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save"></i> Güncelle
                            </button>
                            <a href="@Url.Action("Index")" class="btn btn-secondary">
                                <i class="fas fa-times"></i> İptal
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-info">Duyuru İstatistikleri</h6>
                </div>
                <div class="card-body">
                    <ul class="list-unstyled">
                        <li><i class="fas fa-calendar text-primary"></i> <strong>Oluşturma:</strong> @Model.CreatedDate.ToString("dd.MM.yyyy HH:mm")</li>
                        <li><i class="fas fa-user text-primary"></i> <strong>Oluşturan:</strong> @Model.CreatedBy</li>
                        <li><i class="fas fa-eye text-primary"></i> <strong>Görüntülenme:</strong> @Model.ViewCount</li>
                        @if (Model.UpdatedDate.HasValue)
                        {
                            <li><i class="fas fa-edit text-primary"></i> <strong>Son Güncelleme:</strong> @Model.UpdatedDate?.ToString("dd.MM.yyyy HH:mm")</li>
                        }
                    </ul>
                </div>
            </div>

            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-info">Yardım</h6>
                </div>
                <div class="card-body">
                    <ul class="list-unstyled">
                        <li><i class="fas fa-info-circle text-primary"></i> <strong>Başlık:</strong> Duyurunun ana başlığı</li>
                        <li><i class="fas fa-info-circle text-primary"></i> <strong>Özet:</strong> Kısa açıklama metni</li>
                        <li><i class="fas fa-info-circle text-primary"></i> <strong>İçerik:</strong> Detaylı duyuru metni</li>
                        <li><i class="fas fa-info-circle text-primary"></i> <strong>Etiketler:</strong> Arama için anahtar kelimeler</li>
                        <li><i class="fas fa-info-circle text-primary"></i> <strong>Sabitlenmiş:</strong> Önemli duyurular için kullanın</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    
    <script src="https://cdn.tiny.cloud/1/no-api-key/tinymce/6/tinymce.min.js" referrerpolicy="origin"></script>
    <script>
        tinymce.init({
            selector: '.tinymce-editor',
            height: 300,
            menubar: false,
            plugins: [
                'advlist autolink lists link image charmap print preview anchor',
                'searchreplace visualblocks code fullscreen',
                'insertdatetime media table paste code help wordcount'
            ],
            toolbar: 'undo redo | formatselect | bold italic backcolor | alignleft aligncenter alignright alignjustify | bullist numlist outdent indent | removeformat | help',
            content_style: 'body { font-family:Helvetica,Arial,sans-serif; font-size:14px }'
        });
    </script>
}
